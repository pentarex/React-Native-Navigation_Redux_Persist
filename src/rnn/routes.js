const routes = {
    LOGIN_ROUTE: 'example.LoginView', // if not authenticated
    REGISTER_ROUTE: 'example.RegisterView', // when wants to register
    PROFILE_ROUTE: 'example.ProfileView', // when its logged in
}

export default routes;